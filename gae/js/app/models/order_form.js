// Read-only model that represents an order form on the first page of the order flow.
define(
    ['backbone'],
    function(Backbone) {
        var Model = Backbone.Model.extend({});
        
        return Model;
    }
);
